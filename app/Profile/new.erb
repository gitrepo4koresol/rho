<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>New <%= @profile.title %></h1>
    <a href="<%= url_for :action => :index %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :create %>">
      <input type="hidden" name="id" value="<%= @profile.object %>"/>

      
          <div data-role="fieldcontain">
            <label for="profile[name]" class="fieldLabel">Name</label>
            <input type="text" id="profile[name]" name="profile[name]" <%= placeholder("Name") %> />
          </div>
      
          <div data-role="fieldcontain">
            <label for="profile[interest]" class="fieldLabel">Interest</label>
            <input type="text" id="profile[interest]" name="profile[interest]" <%= placeholder("Interest") %> />
          </div>
      

      <input type="submit" value="Create"/>
    </form>
  </div>

</div>
